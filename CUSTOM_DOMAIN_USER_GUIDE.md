# Руководство по настройке кастомного домена

## Что такое кастомный домен?

Кастомный домен позволяет использовать ваш собственный домен (например, `shop.mysite.com`) вместо субдомена платформы (`my-shop.shops.botmanagertest.online`).

## Выбор типа домена

У вас есть два варианта:

### 1. Субдомен платформы (рекомендуется для начинающих)

**Преимущества:**
- ✅ Бесплатно
- ✅ Автоматическая настройка DNS
- ✅ Автоматический SSL-сертификат
- ✅ Готов к использованию за несколько минут

**Пример:** `my-shop.shops.botmanagertest.online`

**Как настроить:**
1. Введите желаемый slug (например, `my-shop`)
2. Нажмите "Забронировать"
3. Готово! Ваш магазин доступен по адресу

### 2. Кастомный домен (для продвинутых пользователей)

**Преимущества:**
- ✅ Ваш собственный бренд
- ✅ Профессиональный вид
- ✅ Полный контроль над доменом

**Требования:**
- ❗ Нужен собственный домен
- ❗ Доступ к настройкам DNS
- ❗ Базовые знания о DNS

**Пример:** `shop.mysite.com` или `mysite.com`

## Пошаговая инструкция: Настройка кастомного домена

### Шаг 1: Добавьте домен в систему

1. Перейдите в настройки вашего магазина/букинга/страницы
2. Найдите раздел "Кастомный домен"
3. Нажмите кнопку "Добавить домен"
4. Введите ваш домен (например, `shop.example.com`)
5. Нажмите "Добавить"

### Шаг 2: Настройте DNS-записи

После добавления домена вы увидите инструкции по настройке DNS. Вам нужно добавить **A-запись** в настройках вашего домена.

#### Где настраивать DNS?

DNS настраивается в панели управления вашего регистратора домена или хостинг-провайдера:
- **Reg.ru** → Домены → Управление доменом → DNS-серверы и зона
- **Timeweb** → Домены → Управление DNS
- **Cloudflare** → DNS → Records
- **GoDaddy** → DNS Management
- И т.д.

#### Какие данные вводить?

Система покажет вам точные данные, но обычно это выглядит так:

```
Тип:      A
Имя:      @ (для корневого домена) или shop (для поддомена)
Значение: [IP-адрес сервера из инструкции]
TTL:      3600 (или оставьте по умолчанию)
```

#### Примеры настройки

**Пример 1: Корневой домен (`example.com`)**
```
Тип: A
Имя: @
Значение: 1.2.3.4
TTL: 3600
```

**Пример 2: Поддомен (`shop.example.com`)**
```
Тип: A
Имя: shop
Значение: 1.2.3.4
TTL: 3600
```

### Шаг 3: Подождите распространения DNS

После добавления A-записи нужно подождать, пока изменения распространятся:
- **Обычно:** 5-30 минут
- **Максимум:** до 48 часов

💡 **Совет:** Вы можете проверить статус DNS через онлайн-сервисы:
- https://dnschecker.org
- https://www.whatsmydns.net

### Шаг 4: Проверьте DNS в системе

1. Вернитесь в панель управления доменом
2. Нажмите кнопку **"Проверить DNS"**
3. Если DNS настроен правильно, домен активируется автоматически
4. Если есть ошибки, система покажет подсказки

### Шаг 5: Готово!

После успешной проверки:
- ✅ Домен получает статус "Активен"
- ✅ Автоматически выдается SSL-сертификат (HTTPS)
- ✅ Ваш ресурс доступен по кастомному домену

## Часто задаваемые вопросы

### Можно ли использовать Cloudflare?

**Да!** Cloudflare полностью поддерживается. Вы можете:
- Оставить проксирование включенным (оранжевое облако)
- Использовать все функции Cloudflare (кэширование, защита и т.д.)

Просто добавьте A-запись в Cloudflare DNS как обычно.

### Сколько стоит кастомный домен?

Настройка кастомного домена в нашей системе **бесплатна**. Вы платите только за:
- Регистрацию домена у регистратора (обычно 300-1000₽/год)
- Опционально: услуги Cloudflare или другого CDN

### Можно ли использовать несколько доменов?

Да, вы можете добавить несколько кастомных доменов для одного ресурса. Все они будут вести на один и тот же магазин/букинг/страницу.

### Что делать, если DNS не проверяется?

1. **Проверьте правильность данных:**
   - Тип должен быть именно `A` (не CNAME!)
   - IP-адрес должен совпадать с указанным в инструкции
   - Имя записи: `@` для корневого домена или имя поддомена

2. **Подождите больше:**
   - DNS изменения могут занять до 48 часов
   - Попробуйте проверить позже

3. **Проверьте через внешние сервисы:**
   - Используйте https://dnschecker.org
   - Введите ваш домен и проверьте A-запись
   - Она должна указывать на IP из инструкции

4. **Удалите конфликтующие записи:**
   - Убедитесь, что нет других A-записей для этого домена
   - Удалите CNAME записи (они конфликтуют с A-записями)

### Как изменить домен?

1. Удалите старый домен через кнопку "Удалить"
2. Добавьте новый домен
3. Настройте DNS для нового домена

### Что будет, если я удалю домен?

- Домен перестанет работать
- Ресурс останется доступен по субдомену платформы (если настроен)
- Данные не удаляются

## Поддержка

Если у вас возникли проблемы:
1. Проверьте раздел "Часто задаваемые вопросы" выше
2. Убедитесь, что следовали инструкциям точно
3. Проверьте DNS через внешние сервисы
4. Обратитесь в поддержку с описанием проблемы

## Примеры использования

### Интернет-магазин

```
Было:  my-shop.shops.botmanagertest.online
Стало: shop.mystore.com
```

### Система бронирования

```
Было:  my-salon.booking.botmanagertest.online
Стало: booking.mysalon.com
```

### Лендинг

```
Было:  promo.pages.botmanagertest.online
Стало: promo.mycompany.com
```

---

**Успехов в настройке! 🚀**
