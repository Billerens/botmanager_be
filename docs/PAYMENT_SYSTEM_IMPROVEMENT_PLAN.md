# План доработки настроек платежных систем

## 🎯 Цели

1. **Отдельные настройки платежей** для Shop, Booking, CustomPage и Bot
2. **Интеграция оплаты** в соответствующие сущности
3. **API и Flow-узлы** для использования платежей в ботах и кастомных страницах
4. **Тестовые окружения** для проверки настроек

---

## 📊 Часть 1: Изменение архитектуры хранения настроек

### 1.1 Новая структура хранения (Backend)

**Проблема:** Сейчас настройки привязаны к `Bot` через `BotCustomData`, но Shop/Booking/CustomPage могут существовать независимо.

**Решение:** Создать универсальное хранилище настроек с полиморфной связью.

**Сущность PaymentConfig:**

- `id` - UUID
- `entityType` - enum: 'shop' | 'booking_system' | 'custom_page' | 'bot'
- `entityId` - UUID связанной сущности
- `ownerId` - UUID владельца
- `enabled` - boolean
- `testMode` - boolean
- `settings` - JSONB (общие настройки модуля)
- `providers` - string[] (активные провайдеры)
- `providerSettings` - JSONB (зашифрованные настройки провайдеров)
- `createdAt`, `updatedAt`

### 1.2 Миграция данных

Необходимо мигрировать данные из `bot_custom_data` (collection='payment_settings') в новую структуру.

---

## 📊 Часть 2: Сущность Payment (транзакции)

### 2.1 Сущность Payment

Для хранения информации о всех платежах:

- `id` - UUID
- `entityType` - тип источника (shop, booking_system, etc.)
- `entityId` - ID источника
- `targetType` - тип цели (order, booking, api_call, flow_payment, custom)
- `targetId` - ID цели (заказа, бронирования, etc.)
- `provider` - платёжный провайдер
- `externalId` - ID у провайдера (unique)
- `status` - статус платежа
- `amount`, `currency` - сумма и валюта
- `paymentUrl` - URL для оплаты
- `customerData` - данные покупателя (JSONB)
- `metadata` - дополнительные данные (JSONB)
- `statusHistory` - история изменений статуса (JSONB)
- `paidAt`, `canceledAt`, `errorMessage`
- `createdAt`, `updatedAt`

### 2.2 Связь с существующими сущностями

**Order (добавить поля):**

- `paymentId` - UUID, nullable
- `paymentStatus` - enum: NOT_REQUIRED, PENDING, PAID, FAILED, REFUNDED

**Booking (добавить поля):**

- `paymentId` - UUID, nullable
- `paymentStatus` - enum: NOT_REQUIRED, PENDING, PAID, FAILED, REFUNDED
- `paymentAmount` - decimal, nullable

---

## 📊 Часть 3: Сервисы и API

### 3.1 PaymentConfigService

Методы:

- `getConfig(entityType, entityId)` - получение настроек
- `saveConfig(entityType, entityId, config)` - сохранение настроек
- `isPaymentEnabled(entityType, entityId)` - проверка включены ли платежи
- `getEnabledProviders(entityType, entityId)` - получение активных провайдеров

### 3.2 Обновлённый PaymentsService

Новые методы:

- `createPaymentForEntity(entityType, entityId, targetType, targetId, request)`
- `updatePaymentStatus(externalId, status)`
- `getPaymentsByEntity(entityType, entityId)`
- `getPaymentByTarget(targetType, targetId)`

### 3.3 Новые API Endpoints

```
GET/PUT  /payments/config/shop/:shopId
GET/PUT  /payments/config/booking-system/:bookingSystemId
GET/PUT  /payments/config/custom-page/:customPageId
GET/PUT  /payments/config/bot/:botId

POST     /payments/create
GET      /payments/:entityType/:entityId/list
GET      /payments/:paymentId
POST     /payments/test/:entityType/:entityId
```

---

## 📊 Часть 4: Flow-узел для платежей

### 4.1 Новый тип узла PAYMENT

Добавить в `FlowNodeType`:

- `PAYMENT = "payment"`

Данные узла:

- `action` - 'create' | 'check_status' | 'cancel' | 'refund'
- `provider` - провайдер
- `amount` - сумма (может быть переменной)
- `currency` - валюта
- `description` - описание
- `metadata` - метаданные
- `paymentIdVariable` - переменная с ID платежа (для check/cancel/refund)
- `resultVariable` - куда сохранить результат
- `returnUrl`, `cancelUrl` - URL редиректов

### 4.2 PaymentNodeHandler

Обработчик для выполнения действий:

- Создание платежа
- Проверка статуса
- Отмена платежа
- Возврат средств

---

## 📊 Часть 5: Frontend - UI для настройки

### 5.1 Универсальный компонент PaymentSettingsPanel

Переиспользуемый компонент для настройки платежей:

- Props: `entityType`, `entityId`, `onSave`
- Глобальные настройки (вкл/выкл, тестовый режим)
- Список провайдеров с переключателями
- Формы настройки каждого провайдера
- Кнопка тестирования

### 5.2 Интеграция в страницы

| Страница             | Вкладка                  |
| -------------------- | ------------------------ |
| Shop Settings        | "Платежи"                |
| Booking Settings     | "Платежи"                |
| Custom Page Settings | "API Платежи"            |
| Bot Settings         | "Платежи" (существующая) |

### 5.3 Компонент Flow-узла Payment

UI-компонент для редактора Flow:

- Выбор действия
- Выбор провайдера
- Настройка суммы
- Настройка редиректов

---

## 📊 Часть 6: Тестовые окружения

### 6.1 Возможности тестирования по провайдерам

| Провайдер   | Тестовый режим | Способ                  |
| ----------- | -------------- | ----------------------- |
| ЮKassa      | ✅             | Тестовые ключи (test\_) |
| Тинькофф    | ✅             | Тестовый терминал       |
| Robokassa   | ✅             | isTest=true             |
| Stripe      | ✅             | pk*test* / sk*test*     |
| USDT TRC-20 | ✅             | useTestnet=true         |

### 6.2 PaymentTestService

Методы тестирования:

- `runProviderTest(entityType, entityId, provider)` - комплексный тест
- `testConfigValidation(provider, config)` - валидация конфига
- `testApiConnection(provider, config)` - тест соединения
- `testPaymentCreation(provider, config)` - тестовый платёж
- `testWebhookEndpoint(webhookUrl)` - проверка webhook
- `simulateWebhook(entityType, entityId, provider, event)` - симуляция webhook

### 6.3 UI панель тестирования

- Выбор провайдера
- Кнопки: проверить конфиг, проверить соединение, создать тестовый платёж, симулировать webhook
- Отображение результатов тестов
- Рекомендации по исправлению ошибок

---

## 📋 Порядок реализации

### Фаза 1: Основа (Backend) ✅

- [x] 1. Создать сущность `PaymentConfig`
- [x] 2. Создать сущность `Payment`
- [x] 3. Миграция для новых таблиц
- [x] 4. Миграция существующих настроек из `bot_custom_data`
- [x] 5. `PaymentConfigService`
- [x] 6. `PaymentTransactionService` (новый сервис для работы с платежами)

### Фаза 2: Интеграция Shop/Booking (Backend) ✅

- [x] 7. Добавить поля `paymentId`, `paymentStatus` в `Order`
- [x] 8. Добавить поля `paymentId`, `paymentStatus`, `paymentAmount` в `Booking`
- [x] 9. Миграции для новых полей
- [x] 10. Обновить сервисы заказов/бронирований ✅
  - [x] OrdersService: проверка настроек платежей, установка paymentRequired
  - [x] BookingsService: обработчики событий платежей
  - [x] Подписка на события payment.succeeded для автообновления статусов
  - [x] Запрет изменения статуса заказа без оплаты

### Фаза 3: API (Backend) ✅

- [x] 11. Эндпоинты для настроек платежей (`PaymentConfigController`)
- [x] 12. Эндпоинты для управления платежами (`EntityPaymentController`)
- [x] 13. Webhook эндпоинты для каждой сущности (`EntityWebhookController`)

### Фаза 4: Flow-узлы (Backend) ✅

- [x] 14. Добавить тип узла PAYMENT
- [x] 15. Создать `PaymentNodeHandler`
- [x] 16. Интеграция с session

### Фаза 5: Тестирование (Backend) ✅

- [x] 17. `PaymentTestService`
- [x] 18. Эндпоинты для тестирования

### Фаза 6: Frontend - Инфраструктура ✅

- [x] 19. Типы и сервисы для API платежей (`paymentsService.ts`)
- [x] 20. `PaymentSettingsPanel` компонент
- [x] 21. Интеграция в ShopSettings
- [x] 22. Интеграция в BookingSettings (BookingSystemSettings)
- [x] 23. Интеграция в CustomPageSettings (CustomPagesManager)
- [x] 24. BotSettings (сохранён существующий PaymentSettingsTab)
- [x] 25. `PaymentNode` для Flow редактора
- [x] 26. `PaymentTestPanel` компонент
- [x] 27. `PaymentHistory` компонент

### Фаза 7: Frontend - Клиентский UI ⏳ **НЕ РЕАЛИЗОВАНО**

Для полноценной клиентской интеграции необходимо реализовать:

#### 7.1 Обновление типов (критично)

- [ ] 28. Обновить интерфейс `Order` в `publicApiService.ts`:
  ```typescript
  // Добавить поля:
  paymentRequired: boolean;
  paymentStatus: EntityPaymentStatus;
  paymentAmount: number | null;
  paymentId: string | null;
  ```
- [ ] 29. Обновить интерфейс `Order` в `adminApi.ts`
- [ ] 30. Обновить интерфейс `Booking` в `publicApiService.ts`
- [ ] 31. Обновить интерфейс `ShopOrder` в `shopsService.ts`

#### 7.2 Публичный UI для покупателя (Shop)

- [ ] 32. `OrdersSection.tsx` - Добавить отображение статуса оплаты:
  - Показывать бейдж статуса (Ожидает оплаты / Оплачено / Ошибка)
  - Кнопка "Оплатить" для заказов со статусом PENDING
  - Ссылка на страницу оплаты провайдера
- [ ] 33. `OrderPaymentButton` компонент - Кнопка оплаты с выбором провайдера
- [ ] 34. Публичный API метод для получения ссылки на оплату заказа

#### 7.3 Публичный UI для покупателя (Booking)

- [ ] 35. Форма бронирования - Добавить шаг оплаты:
  - Показывать стоимость услуги
  - Выбор провайдера оплаты (если несколько)
  - Редирект на страницу оплаты после создания бронирования
- [ ] 36. Страница "Мои записи" - Показывать статус оплаты бронирований

#### 7.4 Админка для владельца магазина

- [ ] 37. `OrdersTable.tsx` - Добавить колонку "Оплата":
  - Статус оплаты с цветовым индикатором
  - Кнопка "Запросить оплату" (создать платёж)
  - Кнопка "Возврат" для оплаченных заказов
  - Фильтр по статусу оплаты
- [ ] 38. `ShopSettings.tsx` - Добавить вкладку "История платежей":
  - Интегрировать `PaymentHistory` компонент
  - Статистика по платежам (сумма, количество, конверсия)
- [ ] 39. Детали заказа - Показывать информацию о платеже

#### 7.5 Админка для владельца системы бронирования

- [ ] 40. Таблица бронирований - Добавить колонку "Оплата"
- [ ] 41. `BookingSystemSettings.tsx` - Добавить вкладку "История платежей"
- [ ] 42. Детали бронирования - Показывать информацию о платеже

#### 7.6 Уведомления

- [ ] 43. Уведомление владельцу при успешной оплате
- [ ] 44. Уведомление владельцу при ошибке оплаты
- [ ] 45. Email уведомление покупателю (опционально)

#### 7.7 Telegram WebApp интеграция

- [ ] 46. Кнопка оплаты в Telegram Mini App (Shop)
- [ ] 47. Кнопка оплаты в Telegram Mini App (Booking)
- [ ] 48. Обработка возврата из платёжной системы в WebApp

---

## 📊 Часть 7: Детали клиентского UI

### 7.1 Компонент OrderPaymentButton

```typescript
interface OrderPaymentButtonProps {
  shopId: string;
  orderId: string;
  amount: number;
  currency: string;
  providers: PaymentProvider[];
  onPaymentCreated?: (paymentUrl: string) => void;
}
```

Функционал:

- Если один провайдер - сразу создаёт платёж
- Если несколько - показывает модал выбора
- После создания платежа - редирект на paymentUrl
- Обработка ошибок с понятными сообщениями

### 7.2 Статусы оплаты в UI

| Статус       | Цвет   | Текст                 | Действия                 |
| ------------ | ------ | --------------------- | ------------------------ |
| NOT_REQUIRED | gray   | "Оплата не требуется" | -                        |
| PENDING      | orange | "Ожидает оплаты"      | Кнопка "Оплатить"        |
| PAID         | green  | "Оплачено"            | Кнопка "Возврат" (админ) |
| FAILED       | red    | "Ошибка оплаты"       | Кнопка "Повторить"       |
| REFUNDED     | purple | "Возвращено"          | -                        |

### 7.3 API для публичного доступа

Необходимо создать публичные эндпоинты (без авторизации или с токеном заказа):

```
POST /public/shop/:shopId/order/:orderId/pay
  - Создаёт платёж для заказа
  - Возвращает paymentUrl
  - Проверка: заказ существует, принадлежит покупателю

GET /public/shop/:shopId/order/:orderId/payment-status
  - Возвращает текущий статус оплаты
  - Для polling после возврата из платёжки
```

### 7.4 Flow оплаты для покупателя

```
1. Покупатель создаёт заказ
   └─> Заказ создаётся с paymentStatus = PENDING (если платежи включены)

2. Покупатель нажимает "Оплатить"
   └─> Выбор провайдера (если несколько)
   └─> POST /public/shop/:shopId/order/:orderId/pay
   └─> Получение paymentUrl
   └─> Редирект на страницу провайдера

3. Покупатель оплачивает на странице провайдера
   └─> Провайдер отправляет webhook
   └─> PaymentTransactionService обновляет статус
   └─> OrdersService получает событие SUCCEEDED
   └─> Заказ переходит в CONFIRMED

4. Покупатель возвращается в магазин
   └─> returnUrl с параметрами
   └─> Показываем статус оплаты
```

---

## 💡 Технические детали

### Безопасность

- Все секретные ключи шифровать AES-256
- Webhook endpoints защищать подписью
- Rate limiting на API создания платежей
- Логирование всех операций

### Обратная совместимость

- Сохранить работу существующего API для ботов
- Постепенная миграция настроек

### Типы платежей

```typescript
export enum PaymentEntityType {
  SHOP = "shop",
  BOOKING_SYSTEM = "booking_system",
  CUSTOM_PAGE = "custom_page",
  BOT = "bot",
}

export enum PaymentTargetType {
  ORDER = "order",
  BOOKING = "booking",
  API_CALL = "api_call",
  FLOW_PAYMENT = "flow_payment",
  CUSTOM = "custom",
}

export enum PaymentStatus {
  PENDING = "pending",
  WAITING_FOR_CAPTURE = "waiting_for_capture",
  SUCCEEDED = "succeeded",
  CANCELED = "canceled",
  REFUNDED = "refunded",
  FAILED = "failed",
}

export enum EntityPaymentStatus {
  NOT_REQUIRED = "not_required",
  PENDING = "pending",
  PAID = "paid",
  FAILED = "failed",
  REFUNDED = "refunded",
}
```

---

## Созданные файлы

### Backend - Entities

- `backend/src/database/entities/payment-config.entity.ts` - Настройки платежей для сущностей
- `backend/src/database/entities/payment.entity.ts` - Транзакции платежей

### Backend - Migrations

- `backend/src/database/migrations/1700000000045-CreatePaymentConfigsTable.ts`
- `backend/src/database/migrations/1700000000046-CreatePaymentsTable.ts`
- `backend/src/database/migrations/1700000000047-AddPaymentFieldsToOrdersAndBookings.ts`
- `backend/src/database/migrations/1700000000048-MigratePaymentSettingsFromBotCustomData.ts`

### Backend - Services

- `backend/src/modules/payments/services/payment-config.service.ts` - Управление настройками
- `backend/src/modules/payments/services/payment-test.service.ts` - Тестирование провайдеров
- `backend/src/modules/payments/services/payment-transaction.service.ts` - Операции с платежами

### Backend - Controllers

- `backend/src/modules/payments/controllers/payment-config.controller.ts` - API настроек
- `backend/src/modules/payments/controllers/entity-payment.controller.ts` - Операции с платежами
- `backend/src/modules/payments/controllers/entity-webhook.controller.ts` - Webhook для сущностей

### Backend - Node Handlers

- `backend/src/modules/bots/nodes/payment-node.handler.ts` - Flow-узел для платежей

### Обновлённые файлы

- `backend/src/database/entities/order.entity.ts` - Добавлены поля платежей
- `backend/src/database/entities/booking.entity.ts` - Добавлены поля платежей
- `backend/src/modules/bots/dto/flow.dto.ts` - Добавлен тип узла PAYMENT
- `backend/src/modules/bots/nodes/index.ts` - Экспорт PaymentNodeHandler
- `backend/src/modules/bots/flow-execution.service.ts` - Регистрация PaymentNodeHandler
- `backend/src/modules/bots/bots.module.ts` - Импорт PaymentNodeHandler
- `backend/src/modules/payments/payments.module.ts` - Добавлены новые сервисы
- `backend/src/modules/orders/orders.service.ts` - Интеграция с платежами (проверка, события)
- `backend/src/modules/orders/orders.module.ts` - Импорт PaymentsModule
- `backend/src/modules/booking/services/bookings.service.ts` - Интеграция с платежами (события)
- `backend/src/modules/booking/booking.module.ts` - Импорт PaymentsModule

### Frontend - Services

- `frontend/src/services/paymentsService.ts` - Расширен entity-based API

### Frontend - Components

- `frontend/src/components/PaymentSettingsPanel/` - Переиспользуемый компонент настроек платежей
- `frontend/src/components/nodes/PaymentNode/` - Flow-узел для визуального редактора
- `frontend/src/components/PaymentTestPanel/` - Панель тестирования провайдеров
- `frontend/src/components/PaymentHistory/` - История платежей

### Frontend - Обновлённые файлы

- `frontend/src/components/ShopSettings/ShopSettings.tsx` - Добавлена вкладка "Платежи"
- `frontend/src/components/BookingSystemSettings/BookingSystemSettings.tsx` - Добавлена вкладка "Платежи"
- `frontend/src/components/CustomPagesManager/CustomPagesManager.tsx` - Добавлена вкладка "Платежи"
- `frontend/src/components/FlowBuilder/FlowBuilder.tsx` - Добавлен PaymentNode
- `frontend/src/components/FlowBuilder/NodeLibrary.tsx` - Добавлена категория "Платежи"
- `frontend/src/components/FlowBuilder/nodes.utils.ts` - Добавлены defaults для payment
- `frontend/src/types/flow.ts` - Добавлен тип payment

---

## 📁 Файлы для Фазы 7 (клиентский UI)

### Требуется обновить

| Файл                                                                      | Изменения                               |
| ------------------------------------------------------------------------- | --------------------------------------- |
| `frontend/src/services/publicApiService.ts`                               | Добавить поля платежей в Order, Booking |
| `frontend/src/services/adminApi.ts`                                       | Добавить поля платежей в Order          |
| `frontend/src/services/shopsService.ts`                                   | Добавить поля платежей в ShopOrder      |
| `frontend/src/pages/Shop/modules/OrdersSection/OrdersSection.tsx`         | Статус оплаты, кнопка "Оплатить"        |
| `frontend/src/components/OrdersTable/OrdersTable.tsx`                     | Колонка оплаты, фильтр, действия        |
| `frontend/src/components/ShopSettings/ShopSettings.tsx`                   | Вкладка "История платежей"              |
| `frontend/src/components/BookingSystemSettings/BookingSystemSettings.tsx` | Вкладка "История платежей"              |

### Требуется создать

| Файл                                          | Описание                                         |
| --------------------------------------------- | ------------------------------------------------ |
| `frontend/src/components/OrderPaymentButton/` | Компонент кнопки оплаты заказа                   |
| `frontend/src/components/PaymentStatusBadge/` | Бейдж статуса оплаты                             |
| `frontend/src/components/BookingPaymentForm/` | Форма оплаты при бронировании                    |
| `backend/src/modules/public-payments/`        | Публичные эндпоинты для оплаты (без авторизации) |

---

## Дата создания: 2026-01-04

## Дата обновления: 2026-01-04

## Статус: ⚠️ Инфраструктура завершена, клиентский UI не реализован

---

## 📊 Оценка трудозатрат на Фазу 7

| Задача                               | Сложность | Время     | Приоритет     |
| ------------------------------------ | --------- | --------- | ------------- |
| 28-31. Обновление типов              | Низкая    | 1 час     | 🔴 Критично   |
| 32-34. OrdersSection + кнопка оплаты | Средняя   | 3-4 часа  | 🔴 Критично   |
| 35-36. Оплата бронирований           | Средняя   | 3-4 часа  | 🟡 Важно      |
| 37-39. Админка заказов               | Средняя   | 2-3 часа  | 🟡 Важно      |
| 40-42. Админка бронирований          | Средняя   | 2-3 часа  | 🟢 Желательно |
| 43-45. Уведомления                   | Низкая    | 2 часа    | 🟢 Желательно |
| 46-48. Telegram WebApp               | Высокая   | 4-5 часов | 🟢 Желательно |

**Итого: ~18-22 часа** для полной клиентской интеграции

---

## 🔑 Минимально необходимое для запуска

Для базовой работоспособности платежей достаточно:

1. ✅ Обновить типы Order/Booking (пп. 28-31)
2. ✅ Добавить кнопку оплаты в OrdersSection (п. 32)
3. ✅ Добавить статус оплаты в OrdersTable (п. 37)

**Минимум: ~6-8 часов**

---

## Аудит: Проведён 2026-01-04

### Завершено:

- ✅ Backend: все сущности, миграции, сервисы, контроллеры, flow-узлы
- ✅ Frontend: компоненты настройки (PaymentSettingsPanel, PaymentTestPanel, PaymentHistory)
- ✅ Интеграция: OrdersService и BookingsService подключены к платёжной системе

### Не завершено:

- ❌ Клиентский UI для покупателя (кнопка оплаты, статусы)
- ❌ Отображение платежей в админке заказов/бронирований
- ❌ Типы Order/Booking не содержат полей платежей на фронтенде
